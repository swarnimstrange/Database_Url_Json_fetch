import 'package:flutter/material.dart';

class Girl extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Image.asset("assets/b2.png"),
    );
  }
}